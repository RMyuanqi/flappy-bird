/*
* @over     结束图片
* @ready    开始图片
* @title    标题
* */
function Logo(over, ready, title, x, y) {
    this.over = over;
    this.ready = ready;
    this.title = title;
    this.x = x;
    this.y = y;
}